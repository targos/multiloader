import * as permissions from 'https://deno.land/std/permissions/mod.ts';
console.log(permissions);

// TODO: Deno.permissions doesn't seem to exist yet
// permissions.grant({ name: 'net' }).then(() => {
//   console.log('permission granted!');
// });
